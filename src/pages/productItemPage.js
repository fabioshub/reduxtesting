import React, { Component } from 'react';
import ProductItemContainer from '../productItemComponents/productItemContainer';

export default class ProductItemPage extends Component {
    render() {
        return (
            <ProductItemContainer
            />
        );
    }
}